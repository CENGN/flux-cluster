apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atlassian-mysql-pvc
  namespace: atlassian
  labels:
    name: atlassian-mysql-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-ceph-block
