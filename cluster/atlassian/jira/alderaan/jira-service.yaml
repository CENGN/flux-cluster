apiVersion: v1
kind: Service
metadata:
  name: jira-service
  namespace: atlassian
  labels:
    app: jira
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: jira
