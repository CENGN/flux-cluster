apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: confluence-var-pvc
  namespace: atlassian
  labels:
    name: confluence-var-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: rook-ceph-block
